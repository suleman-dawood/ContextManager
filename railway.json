{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cd ContextManager.API && dotnet publish -c Release -o out"
  },
  "deploy": {
    "startCommand": "cd ContextManager.API/out && dotnet ContextManager.API.dll --urls=http://0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

